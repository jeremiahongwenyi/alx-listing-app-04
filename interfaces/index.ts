export interface CardProps {
    title:string
}

export interface ButtonProps {
    text:string
}

export interface PropertyProps {
  name: string;
  address: {
    state: string;
    city: string;
    country: string;
  };
  rating: number;
  category: string[];
  price: number;
  offers: {
    bed: string;
    shower: string;
    occupants: string;
  };
  image: string;
  discount?: string; // optional because sometimes there may not be a discount
}


